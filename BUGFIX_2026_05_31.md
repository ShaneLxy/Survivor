# 问题修复记录

## 修复日期：2026-05-31

### 问题1：回顾剧情按钮不显示

**问题描述：**
- GM工具中第一章第一关已配置剧情对话
- 在已通关该关卡的账号中进入战斗，暂停菜单中没有"回顾剧情"按钮

**原因分析：**
- 原逻辑要求同时满足两个条件：
  1. 关卡已通关 (`isCompleted`)
  2. 关卡有剧情对话 (`hasStoryDialogues`)
- 这个逻辑过于严格，导致即使有剧情也可能不显示按钮

**修复方案：**
- 修改逻辑为：只要关卡有剧情对话就显示"回顾剧情"按钮
- 不再限制是否通关，因为：
  1. 玩家可能想在战斗前重温剧情
  2. 玩家可能首次播放时跳过了，现在想看
  3. 更符合用户体验

**修改文件：**
- `js/ui/views/BattleView.js` - `pauseBattle()` 方法

**修改前：**
```javascript
const isCompleted = this.currentDungeon ? dungeonManager.isCompleted(this.currentDungeon) : false;

// 如果已通关且有剧情，添加"回顾剧情"按钮
if (isCompleted && hasStoryDialogues) {
    buttons.push({ text: '回顾剧情', className: 'btn-secondary', onClick: () => this.reviewStory() });
}
```

**修改后：**
```javascript
// 如果有剧情，添加"回顾剧情"按钮（不限制是否通关）
if (hasStoryDialogues) {
    buttons.push({ text: '回顾剧情', className: 'btn-secondary', onClick: () => this.reviewStory() });
}
```

**测试验证：**
1. 进入配置了剧情的关卡
2. 点击暂停按钮
3. 确认"回顾剧情"按钮显示
4. 点击按钮，确认剧情正常播放

---

### 问题2：点击特技按钮后detail-panel显示错误

**问题描述：**
- 战斗中点击特殊地格后，`battle-detail-panel` 显示该地格信息
- 轮到玩家回合，点击英雄的特技按钮
- 预期：`battle-detail-panel` 应显示特技列表
- 实际：继续显示特殊地格信息

**原因分析：**
- 点击特技按钮时，只设置了 `selectionMode = 'skill'`
- 但没有清除之前保存的状态：
  - `inspectedSpecialTile` - 查看的特殊地格
  - `inspectedObstacle` - 查看的障碍物
  - `inspectedUnitId` - 查看的单位
- 导致渲染时仍然显示旧的信息

**修复方案：**
- 在所有切换行动模式的方法中，清除这些查看状态
- 确保切换到新的行动模式时，detail-panel 显示正确的内容

**修改文件：**
- `js/ui/views/BattleView.js`

**修改的方法：**

1. **selectSkill(skillIndex)** - 选择特技
```javascript
// 添加清除代码
this.inspectedSpecialTile = null;
this.inspectedObstacle = null;
this.inspectedUnitId = null;
```

2. **selectActionMode(mode)** - 选择行动模式（攻击/移动/道具）
```javascript
// 添加清除代码
this.inspectedSpecialTile = null;
this.inspectedObstacle = null;
this.inspectedUnitId = null;
```

3. **openSkillPanel()** - 打开特技面板
```javascript
// 添加清除代码
this.inspectedSpecialTile = null;
this.inspectedObstacle = null;
this.inspectedUnitId = null;
```

4. **useBattleItem(itemId, actorId)** - 使用战斗道具
```javascript
// 添加清除代码（在复活道具分支中）
this.inspectedSpecialTile = null;
this.inspectedObstacle = null;
this.inspectedUnitId = null;
```

**测试验证：**
1. 进入战斗
2. 点击特殊地格，确认 detail-panel 显示地格信息
3. 轮到玩家回合
4. 点击"攻击"按钮，确认 detail-panel 清空或显示攻击相关信息
5. 点击"特技"按钮，确认 detail-panel 显示特技列表
6. 点击"道具"按钮，确认 detail-panel 显示道具列表
7. 点击"移动"按钮，确认 detail-panel 清空或显示移动相关信息

---

## 修复效果

### 问题1修复效果
- ✅ 所有配置了剧情的关卡都会显示"回顾剧情"按钮
- ✅ 不再需要等到通关才能回顾
- ✅ 提升用户体验，随时可以重温剧情

### 问题2修复效果
- ✅ 切换行动模式时，detail-panel 正确更新
- ✅ 不再显示过时的特殊地格/障碍物信息
- ✅ 战斗界面信息显示更加准确和及时

---

## 相关文件

### 修改的文件
- `js/ui/views/BattleView.js`

### 涉及的方法
- `pauseBattle()` - 暂停菜单
- `selectSkill(skillIndex)` - 选择特技
- `selectActionMode(mode)` - 选择行动模式
- `openSkillPanel()` - 打开特技面板
- `useBattleItem(itemId, actorId)` - 使用战斗道具

---

## 技术细节

### 状态管理
战斗视图中维护的查看状态：
- `inspectedUnitId` - 当前查看的单位ID
- `inspectedSpecialTile` - 当前查看的特殊地格
- `inspectedObstacle` - 当前查看的障碍物

这些状态影响 `battle-detail-panel` 的显示内容。

### 清除时机
在以下情况下应该清除查看状态：
1. 切换到攻击模式
2. 切换到移动模式
3. 切换到特技模式
4. 切换到道具模式
5. 选择具体的特技
6. 使用战斗道具

### 渲染逻辑
`renderBattleState()` 方法会根据这些状态决定 detail-panel 显示什么内容：
- 如果有 `inspectedUnitId`，显示单位信息
- 如果有 `inspectedSpecialTile`，显示地格信息
- 如果有 `inspectedObstacle`，显示障碍物信息
- 如果在特技模式，显示特技列表
- 如果在道具模式，显示道具列表
- 否则显示默认内容

---

## 后续建议

### 代码优化
可以考虑创建一个统一的方法来清除查看状态：
```javascript
clearInspectionState() {
    this.inspectedUnitId = null;
    this.inspectedSpecialTile = null;
    this.inspectedObstacle = null;
}
```

然后在需要的地方调用：
```javascript
this.clearInspectionState();
```

这样可以：
1. 减少代码重复
2. 确保清除逻辑一致
3. 方便后续维护

### 测试建议
建议添加以下测试场景：
1. 连续点击不同的特殊地格
2. 点击地格后切换行动模式
3. 点击单位后切换行动模式
4. 在不同行动模式间快速切换
5. 使用特技/道具后的状态

---

**修复完成时间**: 2026-05-31  
**修复人员**: Kiro AI Assistant  
**版本**: v1.1.1
