# 剧情对话手机竖屏布局优化

## 优化日期：2026-05-31

---

## 问题描述

原有的剧情对话布局是为PC端宽屏设计的，在手机竖屏上使用时：
- 立绘和对话框横向排列，空间不足
- 小屏幕（480px以下）直接隐藏立绘
- 对话框不够透明，遮挡背景

---

## 优化方案

### 手机竖屏布局（768px以下）

**改为垂直布局：**
```
┌─────────────────┐
│                 │
│   立绘（上半身）  │  ← 高度200px，只显示上半身
│                 │
├─────────────────┤
│  角色名          │
│                 │
│  对话文本        │  ← 略微透明（0.85）
│                 │
│          [继续]  │
└─────────────────┘
```

### 关键改动

1. **垂直布局**
   - 立绘在上，对话框在下
   - 立绘和对话框无缝连接，形成一个整体

2. **立绘只显示上半身**
   - 使用 `object-fit: cover` 裁剪图片
   - 使用 `object-position: center top` 从顶部开始显示
   - 高度设置为 200px（适中，不占用太多屏幕）

3. **对话框透明度**
   - 从 `rgba(8, 14, 22, 0.95)` 改为 `rgba(8, 14, 22, 0.85)`
   - 更透明，能看到背景

4. **圆角处理**
   - 立绘：上方圆角，下方直角
   - 对话框：上方直角，下方圆角
   - 两者无缝连接

---

## 详细样式说明

### PC端（768px以上）

保持原有的横向布局：
- 立绘在左侧或右侧（200x300px）
- 对话框在另一侧
- 使用 `object-fit: contain` 完整显示立绘

### 手机端（768px以下）

**立绘样式：**
```css
.story-dialogue-avatar {
    width: 100%;              /* 全宽 */
    height: 200px;            /* 固定高度 */
    order: 1;                 /* 显示在上方 */
    border-radius: 12px 12px 0 0;  /* 上方圆角 */
    overflow: hidden;         /* 裁剪超出部分 */
}

.story-dialogue-avatar img {
    object-fit: cover;        /* 裁剪填充 */
    object-position: center top;  /* 从顶部开始显示 */
}
```

**对话框样式：**
```css
.story-dialogue-box {
    order: 2;                 /* 显示在下方 */
    border-radius: 0 0 12px 12px;  /* 下方圆角 */
    background: rgba(8, 14, 22, 0.85);  /* 85%透明度 */
}
```

### 极小屏幕（360px以下）

进一步优化：
- 立绘高度减少到 160px
- 字体大小略微缩小
- 内边距减少

---

## 视觉效果

### PC端效果
```
┌────────────────────────────────────┐
│                                    │
│  ┌────┐  ┌──────────────────┐     │
│  │    │  │ 角色名            │     │
│  │立绘│  │                  │     │
│  │    │  │ 对话文本          │     │
│  │    │  │                  │     │
│  └────┘  └──────────────────┘     │
│                                    │
└────────────────────────────────────┘
```

### 手机端效果
```
┌──────────────┐
│              │
│   立绘上半身   │  ← 200px高
│              │
├──────────────┤
│ 角色名        │
│              │
│ 对话文本      │  ← 透明度85%
│              │
│       [继续]  │
└──────────────┘
```

---

## 技术细节

### object-fit 和 object-position

**PC端：**
```css
object-fit: contain;  /* 完整显示，保持比例 */
```
- 立绘完整显示，不裁剪
- 适合宽屏，有足够空间

**手机端：**
```css
object-fit: cover;           /* 裁剪填充 */
object-position: center top; /* 从顶部开始 */
```
- 裁剪图片，只显示上半身
- 从顶部开始显示，确保显示头部和上半身

### 透明度对比

| 位置 | PC端 | 手机端 |
|------|------|--------|
| 对话框背景 | 0.95 (95%不透明) | 0.85 (85%不透明) |
| 效果 | 几乎不透明 | 略微透明，能看到背景 |

### 动画效果

**PC端：**
- 立绘从左侧或右侧滑入
- 对话框从下方滑入

**手机端：**
- 立绘从上方滑入
- 对话框紧随其后
- 整体从下方滑入

---

## 响应式断点

| 屏幕宽度 | 布局方式 | 立绘尺寸 | 对话框透明度 |
|---------|---------|---------|-------------|
| > 768px | 横向布局 | 200x300px | 0.95 |
| 361-768px | 垂直布局 | 100%x200px | 0.85 |
| ≤ 360px | 垂直布局 | 100%x160px | 0.85 |

---

## 使用建议

### 立绘图片要求

**PC端和手机端通用：**
- **格式**：PNG（支持透明背景）
- **尺寸**：建议 400x600 像素或更高
- **内容**：完整的上半身立绘（头部到腰部）

**手机端特别注意：**
- 确保角色的头部和上半身在图片的**上半部分**
- 重要内容（脸部、表情）应该在图片顶部 1/3 区域
- 避免重要内容在图片下半部分（会被裁剪）

### 立绘构图建议

**推荐构图：**
```
┌─────────┐
│  头部    │  ← 顶部 1/3，最重要
├─────────┤
│  上半身  │  ← 中间 1/3，会显示
├─────────┤
│  腰部    │  ← 底部 1/3，手机端会被裁剪
└─────────┘
```

**不推荐：**
- 头部在图片中间或下方
- 重要表情或细节在图片下半部分

---

## 测试验证

### 测试步骤

1. **PC端测试**（浏览器宽度 > 768px）
   - 立绘应该在对话框左侧或右侧
   - 立绘完整显示
   - 对话框几乎不透明

2. **手机端测试**（浏览器宽度 < 768px）
   - 立绘应该在对话框上方
   - 立绘只显示上半身
   - 对话框略微透明，能看到背景
   - 立绘和对话框无缝连接

3. **极小屏幕测试**（浏览器宽度 < 360px）
   - 立绘高度更小（160px）
   - 文字大小适中，可读性良好

### 测试工具

**Chrome DevTools：**
1. 按 F12 打开开发者工具
2. 点击"Toggle device toolbar"（Ctrl+Shift+M）
3. 选择不同的设备预设：
   - iPhone SE (375x667) - 小屏幕
   - iPhone 12 Pro (390x844) - 中等屏幕
   - iPad (768x1024) - 平板
   - Desktop (1920x1080) - PC

---

## 常见问题

### Q1: 立绘显示不完整或被裁剪太多

**原因**：立绘图片的构图不适合手机端

**解决方案**：
1. 调整立绘图片，确保重要内容在上半部分
2. 或者调整 CSS 中的 `height` 值（第 220 行）

### Q2: 对话框太透明，文字看不清

**原因**：背景太亮或太复杂

**解决方案**：
1. 增加对话框透明度：将 `0.85` 改为 `0.90` 或 `0.95`
2. 或者增加文字阴影

### Q3: 立绘高度不合适

**解决方案**：
调整 CSS 中的立绘高度：

```css
/* 手机端 */
.story-dialogue-avatar {
    height: 200px;  /* 调整这个值 */
}

/* 极小屏幕 */
.story-dialogue-avatar {
    height: 160px;  /* 调整这个值 */
}
```

### Q4: 想要更透明的对话框

**解决方案**：
修改对话框背景透明度：

```css
.story-dialogue-box {
    background: linear-gradient(135deg, 
        rgba(8, 14, 22, 0.75) 0%,    /* 改为 0.75 更透明 */
        rgba(18, 28, 42, 0.75) 100%
    );
}
```

---

## 修改的文件

- `css/story-dialogue.css` - 剧情对话样式

---

## 优化效果对比

### 优化前

| 问题 | 影响 |
|------|------|
| ❌ 横向布局，空间不足 | 立绘和对话框挤在一起 |
| ❌ 小屏幕完全隐藏立绘 | 失去视觉表现力 |
| ❌ 对话框不透明 | 完全遮挡背景 |

### 优化后

| 改进 | 效果 |
|------|------|
| ✅ 垂直布局，空间充足 | 立绘和对话框都有足够空间 |
| ✅ 立绘只显示上半身 | 保留视觉表现，节省空间 |
| ✅ 对话框略微透明 | 能看到背景，更有沉浸感 |
| ✅ 无缝连接 | 立绘和对话框形成整体 |

---

## 总结

这次优化专门针对手机竖屏使用场景，将原有的横向布局改为垂直布局，并通过 `object-fit: cover` 和 `object-position: top` 实现了"只显示上半身"的效果。同时增加了对话框的透明度，让整体视觉效果更加轻盈和沉浸。

**核心改进：**
1. 垂直布局，适合手机竖屏
2. 立绘只显示上半身，节省空间
3. 对话框略微透明，不完全遮挡背景
4. 无缝连接，视觉更统一

---

**优化完成时间**: 2026-05-31  
**优化人员**: Kiro AI Assistant  
**版本**: v1.3.0
